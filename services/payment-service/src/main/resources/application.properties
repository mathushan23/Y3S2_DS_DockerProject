# # spring.application.name=payment-service
# # server.port=${SERVER_PORT:8085}

# # stripe.api.key=${STRIPE_API_KEY:your-default-test-key-here}

# # # # Stripe Configuration
# # # stripe.api.key=${STRIPE_SECRET_KEY:your_stripe_secret_key_here}

# spring.application.name=payment-service
# server.port=${SERVER_PORT:8085}

# # Stripe API Key - loaded from environment variable (never hardcode here)
# stripe.api.key=${STRIPE_API_KEY}

# # Optional: only for local testing if you really want a fallback (but better to avoid any sk_test_ in the file)
# # stripe.api.key=${STRIPE_API_KEY:sk_test_placeholder_do_not_use_in_production}